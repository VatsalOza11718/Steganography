"""Top-level package for the BIS steganography toolkit.

The package is split into three main areas:
- ``bis.stego``: hide and recover payloads from text, audio, video, and images
- ``bis.generation``: image-generation helpers used by advanced workflows
- ``bis.fine_tuning``: adapt those models to domain-specific data
"""

# Package version is kept here so CLIs, APIs, and packaging metadata can
# all reference the same value.
__version__ = "0.3.0"
